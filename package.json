{
  "name": "stylingcoach",
  "version": "1.0.0",
  "scripts": {
    "build": "cd client && npm install && npm run build && cd .. && cp -r client/dist dist",
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
