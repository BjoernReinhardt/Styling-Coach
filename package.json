{
  "name": "stylingcoach",
  "version": "1.0.0",
  "scripts": {
    "build": "npm install --prefix client && npm run build --prefix client",
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
